export const Hero = () => {
  return (
    <section className="relative w-full h-screen bg-black overflow-hidden">
      {/* Video placeholder — drop your video element here */}
    </section>
  );
};
